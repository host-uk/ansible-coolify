all:
  children:
    # Environment groups: Use these with --limit (e.g., -l production)
    production:
      hosts:
        noc.host.uk.com:
        de.host.uk.com:
        de2.host.uk.com:
        build.de.host.uk.com:
    development:
      hosts:
        vm-worker:
          ansible_host: vm-worker
          ansible_ssh_private_key_file: ~/.ssh/vm-worker
          ansible_become: true

    # Role-based groups
    app_servers:
      hosts:
        de.host.uk.com:
        de2.host.uk.com:
    monitoring:
      hosts:
        noc.host.uk.com:
    builders:
      hosts:
        build.de.host.uk.com:

    # Functional groups: Used by playbooks
    controller:
      hosts:
        noc.host.uk.com:
        lab.snider.dev:
          ansible_host: 10.69.69.87
        localhost:
          ansible_connection: local
          ansible_become: false
    worker:
      hosts:
        de.host.uk.com:
        de2.host.uk.com:
        build.de.host.uk.com:
        vm-worker:
          ansible_host: vm-worker
          ansible_ssh_private_key_file: /Users/snider/.ssh/vm-worker

