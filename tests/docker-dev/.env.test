# Coolify API Test Configuration
#
# Setup:
#   1. Copy this file: cp .env.test .env.test.local
#   2. Set your API token in .env.test.local
#   3. Run tests: make dev-test-service SERVICE=uptime-kuma
#
# To get a new API token:
#   docker exec coolify-dev-controller docker exec coolify php artisan tinker --execute='
#     $user = \App\Models\User::first();
#     $token = $user->createToken("ansible-test", ["*"], null, 1);
#     echo $token->plainTextToken;
#   '
#
# This template is checked into git. Your .env.test.local is gitignored.

export COOLIFY_API_URL="http://172.28.0.10:8000/api/v1"
export COOLIFY_API_TOKEN=""
