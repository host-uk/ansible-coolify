# Service definition for: meilisearch
# Auto-generated from successful service test
#
# Apply with:
#   ansible-playbook playbooks/coolify/template/apply.yml \
#     -e template_name=meilisearch -e project=xxx -e env_name=yyy

services:
  - service_type: meilisearch
    description: ""
    connect_to_docker_network: false
    docker_compose_raw: |
        services:
          meilisearch:
            image: 'getmeili/meilisearch:latest'
            environment:
              - SERVICE_URL_MEILISEARCH_7700
              - 'MEILI_NO_ANALYTICS=${MEILI_NO_ANALYTICS:-true}'
              - 'MEILI_ENV=${MEILI_ENV:-production}'
              - 'MEILI_MASTER_KEY=${SERVICE_PASSWORD_MEILISEARCH}'
            volumes:
              - 'meilisearch-data:/meili_data'
            healthcheck:
              test:
                - CMD
                - curl
                - '-f'
                - 'http://127.0.0.1:7700/health'
              interval: 2s
              timeout: 10s
              retries: 15

